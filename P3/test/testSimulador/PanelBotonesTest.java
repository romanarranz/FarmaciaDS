package testSimulador;

public class PanelBotonesTest {

	
	/*
	 * Comprobar que los objeetos que se han creado en la interfaz se han creado de forma correcta,
	 * es decir comprobamos que no sean null y comprobamos que son instancias de la clases corresponendientes.
	 * 
	 * 
	 * 
	 * Interfaz i = new Interfaz();
	 * 
	 * assertNotNull(interfaz.getSimulacion());
	 * assertTrue(interfaz.getSimulacion().instanceOf(Simulacion));
	 * assertNotNull(interfaz.getPanelBotones());
	 * assertSame(interfaz.getSimulacion(), interfaz.getPanelBotones().getSimulacion()); // comprpeuba que la referencia a memoria es la misma desde dos sitios disintos
	 */
}
